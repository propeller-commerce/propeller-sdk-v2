/**
 Input object for CartAcceptPurchaseAuthorizationRequestInput
 */
export interface CartAcceptPurchaseAuthorizationRequestInput {
  /** Contact unique identifier. The contact has to be an authorization manager within the same company as the one set on this cart */
  contactId?: number;
}