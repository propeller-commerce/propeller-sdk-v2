/**
 * @enum ProductBulkMoveErrorCode
 * Error codes for product bulk move operations
 */
export enum ProductBulkMoveErrorCode {
  VALIDATION = "VALIDATION",
  DB_INSERT = "DB_INSERT",
  UNKNOWN = "UNKNOWN",
}
