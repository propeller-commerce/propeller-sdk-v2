/**
 * @enum ClusterBulkMoveErrorCode
 * Error codes for cluster bulk move operations
 */
export enum ClusterBulkMoveErrorCode {
  VALIDATION = "VALIDATION",
  DB_INSERT = "DB_INSERT",
  UNKNOWN = "UNKNOWN",
}
