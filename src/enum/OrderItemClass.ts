/**
 * @enum OrderItemClass
 Class enumeration for OrderItem categorization
 */
export enum OrderItemClass {
  product = "product",
  incentive = "incentive",
  surcharge = "surcharge",
  postage = "postage",
  payment = "payment",
}