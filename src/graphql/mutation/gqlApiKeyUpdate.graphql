mutation gqlApiKeyUpdate($id: String!, $input: GqlApiKeyUpdateInput!) {
  gqlApiKeyUpdate(id: $id, input: $input) {
    id
    name
    key
    active
    channelId
    createdAt
    lastModifiedAt
  }
}
