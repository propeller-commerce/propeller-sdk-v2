query user($id: Int, $login: String) {
  user(id: $id, login: $login) {
    __typename
    ... on Contact {
        ... ContactFields
    }
    ... on Customer {
        ... CustomerFields
    }
  }
}
