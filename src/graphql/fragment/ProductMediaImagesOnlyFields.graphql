fragment ProductMediaImagesOnlyFields on ProductMedia {
  images(search: $imageSearchFilters) {
    items {
      ... MediaImageFields
    }
    itemsFound
    offset
    page
    pages
    start
    end
  }
}