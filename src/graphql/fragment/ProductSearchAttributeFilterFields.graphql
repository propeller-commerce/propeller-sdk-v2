fragment ProductSearchAttributeFilterFields on ProductSearchAttributeFilter {
  id
  type
  textFilters {
    value
    count
    countTotal
    countActive
    isSelected
  }
  integerRangeFilter {
    min
    max
  }
  decimalRangeFilter {
    min
    max
  }
}
