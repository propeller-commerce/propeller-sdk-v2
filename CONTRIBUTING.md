# Contributing

## Closed architectural decisions

The following are **settled**. PRs that reopen them without prior written
maintainer agreement will be closed. If you believe one must change, open an
issue first and make the case there — do not start the work.

### Type model: plain interfaces (FINAL)

Every response and input type in `src/type/*.ts` is a plain TypeScript
`interface`; every `src/enum/*.ts` is a plain `enum`. There are **no wrapper
classes, no constructors, no generated getter methods**. Service methods
return plain JSON values typed as these interfaces — no runtime wrapping, so
`JSON.parse`/`JSON.stringify` round-trips (Redux, SSR hydration, IndexedDB)
work without rewrapping.

This was the deliberate v0.10.0 architecture. A prior class + getter design
existed (0.7.0–0.9.x) and was removed; the type model is not to be flipped
again. Do **not** reintroduce classes, getters, branded/nominal wrappers, or
per-operation `Pick<>` / `<Op>Result` / `Partial<T>` projection-type codegen.

Localized fields are `LocalizedString[]`; read them with the `getLocalized()`
helper. There are no `getName(language)`-style resolver methods.

### Partial responses are expected, not a bug

A method returns the operation's named GraphQL type, but only the fields that
operation's document selects are populated. Other fields — including ones the
interface declares non-optional — are `undefined` at runtime, and TypeScript
will not warn. This is a **known, accepted trade-off**, documented in the
README "Return types" section. It is intentionally solved with documentation,
not with types or codegen.

### Generated artifacts are committed and drift-guarded

`src/generated/**` (≈460 per-operation documents plus the variable barrels) is
committed source-of-truth, regenerated by `npm run build:graphql`, and
verified by `npm run check:drift`. Never `rimraf src/generated` in a
default/install hook, and always run `npm run check:drift` after touching
anything under `src/graphql/` or the generator.

### Guarded `prepare` script (FINAL)

`package.json` runs `prepare: node scripts/prepare.js`. This hook is
**guarded and non-destructive**: it no-ops when a built `dist/` is already
present (registry tarball or prior build), builds only when sources are present
but `dist/` is missing (git-URL / local install), and otherwise stays silent.

`prepare` must stay — it is the only npm lifecycle hook that runs on a
`github:…#master` install, which is how the deliberately-unreleased WIP is
consumed (`prepack`/`prepublishOnly` only run on pack/publish). Do **not**
replace it with a bare `prepare: "npm run build"` or any unconditional
`clean`/rebuild: that destructive form broke `--install-links` consumers
(removed in `b855f43`; the guarded form was deliberately restored in
`a046cac`).

## Commit conventions

Do **not** add a `Co-Authored-By` trailer to commits in this repo.
